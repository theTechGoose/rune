// SPEC-OWNED — regenerated by `rune sync`. Edit the .rune spec, not this file.

export abstract class BookBase {
  abstract fill(title: unknown): unknown;
  abstract save(bookDto: unknown): unknown;
  abstract toDto(): unknown;
  abstract load(id: unknown): unknown;
  abstract markBorrowed(): unknown;
}
