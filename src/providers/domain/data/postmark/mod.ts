// Scaffolded once; fill in the bodies. `sync` preserves this file.
import { PostmarkBase } from "./sig.ts";

export class Postmark extends PostmarkBase {
  override sendEmail(notifyDto: unknown): unknown {
    throw new Error("not implemented");
  }
}
