// Scaffolded once; fill in the bodies. `sync` preserves this file.
import { RecordsBase } from "./sig.ts";

export class Records extends RecordsBase {
  override buildLeg(submitRequestDto: unknown): unknown {
    throw new Error("not implemented");
  }
}
