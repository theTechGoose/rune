// Scaffolded once; fill in the bodies. `sync` preserves this file.
import { SoldOutBase } from "./sig.ts";

export class SoldOut extends SoldOutBase {
  override lookup(calendarKeyDto: unknown): unknown {
    throw new Error("not implemented");
  }
}
