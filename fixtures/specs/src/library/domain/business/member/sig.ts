// SPEC-OWNED — regenerated by `rune sync`. Edit the .rune spec, not this file.

export abstract class MemberBase {
  abstract fill(name: unknown): unknown;
  abstract save(memberDto: unknown): unknown;
  abstract toDto(): unknown;
}
