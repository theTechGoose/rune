// Scaffolded once; fill in the bodies. `sync` preserves this file.
import { HiPartnerBase } from "./sig.ts";

export class HiPartner extends HiPartnerBase {
  override cancel(cancelRequestDto: unknown): unknown {
    throw new Error("not implemented");
  }
}
