// Scaffolded once; fill in the bodies. `sync` preserves this file.
import { CalendarBase } from "./sig.ts";

export class Calendar extends CalendarBase {
  override fetch(sessionTokenDto: unknown): unknown {
    throw new Error("not implemented");
  }
}
