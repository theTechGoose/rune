// Scaffolded once; fill in the bodies. `sync` preserves this file.
import { SubmitBase } from "./sig.ts";

export class Submit extends SubmitBase {
  override send(submitInputDto: unknown): unknown {
    throw new Error("not implemented");
  }
}
